pipeline:
#  docker:
#    image: plugins/docker
#    username: daixk1212
#    password: dxk19911212
#    repo: test
#    tags: latest
  build:
    image: maven:slim
    commands:
      - docker run -it --rm --name ci-cd-demo -v "$(pwd)":/usr/src/mymaven -v /data/maven:/usr/share/maven/ref -w /usr/src/mymaven maven:slim mvn clean package -DskipTests
