kind: pipeline
type: docker
name: ci-cd-demo

volumes:
  - name: maven_local_repo
    host:
      path: /app/data/maven_local_repo

steps:
  - name: build
    image: maven:3-jdk-8
    volumes:
      - name: maven_local_repo
        path: /root/.m2
    commands:
      - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
      - mvn test -B
  - name: deploy
    image: appleboy/drone-ssh
    settings:
      host: 112.13.201.108
      username: root
      port: 2022
      key:
        from_secret: ssh_key
      command_timeout: 30m
      script_stop: false
      script:
        - cd ~ & docker-compose ps
        - cat /app/test/deploy.sh
        - cat /app/test/Dockerfile